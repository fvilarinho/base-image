version: '3.8'

services:
  base-image:
    build:
      context: .
    image: ghcr.io/concepting-com-br/base-image:1.0.0
    container_name: base-image
    hostname: base-image
    depends_on:
      - etcd
      
  etcd:
    image: ghcr.io/concepting-com-br/etcd:1.0.0
    container_name: etcd
    hostname: etcd
    ports: 
      - 2379:2379
    volumes:
      - ./etc/default.json:/home/user/etc/app.json
      - etcd-data:/home/user/data

volumes:
  etcd-data:
  